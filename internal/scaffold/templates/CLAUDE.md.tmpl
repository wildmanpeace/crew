@AGENTS.md

## This project

<!-- Describe what this project is, in a sentence or two. The first mate reads
     this before every interview. -->

- Build: `{{.BuildCommand}}`
- Test: `{{.TestCommand}}`

## Claude Code specifics

`AGENTS.md` above defines the first mate role and is the operative document.
Claude Code reads `CLAUDE.md` rather than `AGENTS.md`, which is why this file
imports it.
